# Demo

Turn a change in an npm web project into a shareable demo page: Playwright tests per
acceptance criterion, recorded videos and screenshots, before/after screenshots against
the base branch, and an acceptance-criteria checklist backed by test results.

See `skills/demo/SKILL.md`. Only applies to projects with a `package.json` that can serve
the app locally (a `dev`, `start` or `preview` script).
