# Coolify

Operate self-hosted Coolify servers over SSH: check application/container status,
inspect disk and volume usage, create volumes, read logs, and troubleshoot deployments
and the proxy.

See `skills/coolify-ops/SKILL.md`. Server connection details (SSH host aliases) live in
`~/.ssh/config` on the operator's machine, never in this plugin.
